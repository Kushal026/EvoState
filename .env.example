# EvoState Environment Configuration Template
# For production deployment on Vercel:
# Set NEXT_PUBLIC_API_BASE to your deployed FastAPI backend URL (if using external server),
# or leave empty to utilize native, zero-latency client-side simulation.

NEXT_PUBLIC_API_BASE=
PORT=3000
